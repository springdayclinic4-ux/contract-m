# 다음 작업 가이드

## 🎯 현재 상태

✅ **완료된 작업**
- Node.js 24 + Fastify 백엔드 서버 구축
- PostgreSQL 18 + Prisma 5.19.1 설정
- 인증 API 구현 (회원가입, 로그인, 이메일 인증)
- 서버 실행 성공 (http://localhost:3000)

## 📋 다음 단계

### 1단계: API 테스트 (즉시 가능)

#### Health Check 테스트
브라우저에서 접속:
```
http://localhost:3000/health
```

#### API 테스트 도구 사용
- **Postman** 설치 및 API 테스트
- **Thunder Client** (VS Code 확장) 사용
- **curl** 명령어로 테스트

예시:
```bash
# Health Check
curl http://localhost:3000/health

# 이메일 인증 코드 발송
curl -X POST http://localhost:3000/api/auth/send-verification \
  -H "Content-Type: application/json" \
  -d "{\"email\": \"test@example.com\"}"
```

### 2단계: AWS SES 설정 (이메일 발송용)

현재 이메일 발송 기능은 AWS SES를 사용하도록 구현되어 있지만, AWS 자격 증명이 필요합니다.

#### 설정 방법:
1. AWS 계정 생성
2. SES (Simple Email Service) 설정
3. 발신 이메일 인증
4. `.env` 파일에 AWS 자격 증명 추가:
```env
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_SES_FROM_EMAIL=noreply@yourdomain.com
```

**또는** 테스트용으로 다른 이메일 서비스 사용:
- Nodemailer + Gmail
- SendGrid
- Mailgun

### 3단계: 프론트엔드 개발

#### React + TypeScript 프론트엔드 설정
```bash
cd frontend
npm install
npm run dev
```

#### 구현할 페이지:
1. **회원가입 페이지**
   - 병원 회원가입
   - 의사 회원가입
   - 일반직원 회원가입
   - 이메일 인증

2. **로그인 페이지**
   - 이메일/비밀번호 로그인
   - 사용자 유형 선택

3. **대시보드**
   - 계약서 목록
   - 통계 정보

### 4단계: 계약서 관리 API 구현

#### 필요한 API:
```javascript
// 일용직 근로계약서
POST   /api/contracts              // 계약서 생성
GET    /api/contracts              // 계약서 목록
GET    /api/contracts/:id          // 계약서 상세
PATCH  /api/contracts/:id          // 계약서 수정
DELETE /api/contracts/:id          // 계약서 삭제
POST   /api/contracts/:id/invite   // 계약서 초대
POST   /api/contracts/:id/sign     // 계약서 서명

// 일반 근로계약서
POST   /api/labor-contracts              
GET    /api/labor-contracts              
GET    /api/labor-contracts/:id          
PATCH  /api/labor-contracts/:id          
DELETE /api/labor-contracts/:id          
POST   /api/labor-contracts/:id/invite   
POST   /api/labor-contracts/:id/sign     
```

#### 구현 파일:
- `src/routes/contracts.js`
- `src/routes/labor-contracts.js`
- `src/schemas/contract.schema.js`

### 5단계: PDF 생성 기능

#### PDFKit 사용
```javascript
import PDFDocument from 'pdfkit';

// 계약서 PDF 생성
function generateContractPDF(contract) {
  const doc = new PDFDocument();
  // PDF 내용 작성
  return doc;
}
```

### 6단계: 파일 업로드 (AWS S3)

#### S3 설정
1. AWS S3 버킷 생성
2. IAM 사용자 생성 및 권한 부여
3. 파일 업로드 API 구현

```javascript
// 파일 업로드 엔드포인트
POST /api/upload
```

### 7단계: 통계 및 대시보드 API

```javascript
GET /api/statistics/overview        // 전체 통계
GET /api/statistics/contracts       // 계약서 통계
GET /api/statistics/monthly         // 월별 통계
```

## 🛠️ 유용한 명령어

### 개발 중
```bash
# 서버 실행 (자동 재시작)
npm run dev

# Prisma Studio (데이터베이스 GUI)
npm run db:studio

# 데이터베이스 초기화
npm run db:migrate -- --name reset
```

### 문제 해결
```bash
# Prisma 클라이언트 재생성
npm run db:generate

# node_modules 재설치
rm -rf node_modules package-lock.json
npm install

# 데이터베이스 연결 확인
npm run db:studio
```

## 📚 참고 자료

### 공식 문서
- [Fastify](https://fastify.dev/)
- [Prisma](https://www.prisma.io/docs)
- [Zod](https://zod.dev/)
- [React](https://react.dev/)
- [TypeScript](https://www.typescriptlang.org/)

### 프로젝트 문서
- `기획서.md` - 전체 프로젝트 기획
- `HOS_Data_Structure.md` - HOS 데이터 구조
- `UI-UX플로우.md` - UI/UX 플로우
- `개발진행상황.md` - 개발 진행 상황

## 🎯 우선순위

### 높음 (즉시 시작)
1. ✅ API 테스트
2. 프론트엔드 로그인/회원가입 페이지
3. 계약서 생성 API

### 중간 (1-2주 내)
1. 계약서 관리 기능
2. PDF 생성
3. 이메일 발송 (AWS SES)

### 낮음 (추후)
1. 통계 및 대시보드
2. 파일 업로드
3. AWS 배포

## 💡 팁

1. **API 테스트**: Postman이나 Thunder Client로 모든 API를 테스트하세요
2. **Git 커밋**: 기능별로 커밋하면서 개발하세요
3. **환경 변수**: `.env` 파일은 Git에 커밋하지 마세요 (`.gitignore`에 추가됨)
4. **데이터베이스**: Prisma Studio로 데이터를 확인하세요
5. **에러 처리**: 모든 API에 적절한 에러 처리를 추가하세요

## 🚀 시작하기

어떤 작업부터 시작할까요?

1. **API 테스트** - 구현된 인증 API 테스트
2. **프론트엔드** - React 프론트엔드 개발 시작
3. **계약서 API** - 계약서 관리 API 구현
4. **AWS 설정** - 이메일 발송 및 파일 업로드 설정

원하는 작업을 선택하시면 자세한 가이드를 제공해드리겠습니다!
